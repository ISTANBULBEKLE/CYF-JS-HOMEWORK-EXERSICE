
let names=["Angela", "Ben", "Jenny", "Cloe"];

function whosPaying(names){
    
    return `${names[Math.floor(Math.random() * names.length)]} is going to buy lunch today!`;
} 


/*
let names=["Angela", "Ben", "Jenny", "Cloe"];

function whosPaying(names) {
    return names[Math.floor(Math.random()*names.length)];
}
whosPaying(names);

*/